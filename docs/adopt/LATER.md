# Later tranche

Sites that work today and need more than a layout swap, or products
whose accent is provisional. Do not start these until the six above are
shipped and the package has settled.

- **grapevine** (`GrapevineNetwork/website`, Next.js, gvn.au) — real
  marketing content, a green/purple brand the accent file already
  follows, plus pages the shell has no component for yet (a pricing
  table with credits, an employer/seeker split). Port to Astro on the
  shell; expect to add one or two components to the package (a
  two-audience hero, a credits table). The mobile app store links go in
  the hero's secondary CTA.
- **Pay N Tally** (`inflow-tech/payntally`, Astro) — layout swap like
  runsheet; accent `payntally.css` is from its brand values. The
  compliance/how-it-works pages become `Docs`.
- **1099-W9** (`1099-w9/replit-site`, Vite + React) — a rebuild from the
  marketing assets in `1099-w9/marketing` (web copy bundle, FAQ, how it
  works, email sequence); accent `ten99.css` from the site's green.
- **inflow** (myinflow.com) — no recorded brand; `inflow.css` is
  provisional (harbour teal → amber). Confirm the accent with Chris
  before building.
- **optrader** (`optradercards/site` is the app, not a site) — needs a
  marketing site from scratch; `optrader.css` (tangerine) is provisional.
- **thirtysixzero**, **Project Mesh** — black-on-white Next.js and a raw
  HTML site respectively; accents provisional (steel; mesh blue from the
  existing site's variables). Confirm, then rebuild on the template.
