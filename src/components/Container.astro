---
/** The page column: 72rem, 1.5rem gutters (2rem from md). */
interface Props { class?: string; narrow?: boolean }
const { class: cls = "", narrow = false } = Astro.props;
---
<div class:list={["mx-auto w-full px-6 md:px-8", narrow ? "max-w-prose" : "max-w-page", cls]}>
  <slot />
</div>
