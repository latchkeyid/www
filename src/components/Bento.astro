---
/** A bento: 6-column grid; cells say how many columns/rows they take. Size = importance, or don't use it. */
interface Props { class?: string }
const { class: cls = "" } = Astro.props;
---
<div class:list={["grid auto-rows-[minmax(11rem,auto)] gap-4 md:grid-cols-6", cls]}>
  <slot />
</div>
