---
interface Props { value: string; label: string }
const { value, label } = Astro.props;
---
<div class="reveal bg-card p-6">
  <dd class="font-display text-4xl tabular-nums md:text-5xl">{value}</dd>
  <dt class="mt-1 text-sm text-muted-foreground">{label}</dt>
</div>
